// Filename: UiLayer.js
/**
 * Data object containing data of markers and popups on the map.
 */
define([
  'jquery', 
  'underscore', 
  'backbone',
  'leaflet'
], function($, _, Backbone, L){
	
	var UiLayer = Backbone.Model.extend({
		
	});
	
	return UiLayer;
});